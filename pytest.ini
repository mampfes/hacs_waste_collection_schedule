[pytest]
norecursedirs = custom_components/waste_collection_schedule/waste_collection_schedule/test
filterwarnings = ignore:.*:DeprecationWarning
python_files = test_source_components.py test_new_architecture.py test_offline_fixtures.py
markers =
    live: test fetches from a live provider endpoint; excluded from gating CI (run with `-m live` locally). Replaced by offline fixtures.
