# Per-source overrides for msva_se (en).
---
params:
  additional_information:
    description: House letter or unit identifier, e.g. 'A'. Leave empty if not applicable.
    label: Additional information
  postcode:
    description: Five-digit Swedish postal code, e.g. '85461'.
    label: Postal code
