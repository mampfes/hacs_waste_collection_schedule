# Per-source overrides for saver_nl (en).
# Generated by scripts/migrate_i18n.py from the original PARAM_TRANSLATIONS
# and PARAM_DESCRIPTIONS dicts. Edit freely.
---
params:
  postcode:
    description: Dutch postal code (4 digits + 2 letters), e.g. 4702AA
    label: Postal code
  toevoeging:
    description: House letter or addition (only required when more than one address
      shares the postcode/huisnummer)
    label: Addition
