# Per-source overrides for c_trace_de (en).
# Generated by scripts/migrate_i18n.py from the original PARAM_TRANSLATIONS
# and PARAM_DESCRIPTIONS dicts. Edit freely.
---
params:
  abfall:
    description: Pipe-separated waste type IDs to fetch (e.g. '0|1|2|5'). Leave empty
      to fetch all types. Visit your provider's calendar page to see which IDs correspond
      to which waste types.
    label: Waste type IDs
  hausnummer:
    label: House number
  ort:
    label: District
  ortsteil:
    label: Subdistrict
  service:
    label: Operator
