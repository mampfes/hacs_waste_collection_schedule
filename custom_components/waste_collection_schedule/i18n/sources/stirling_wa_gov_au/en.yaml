# Per-source overrides for stirling_wa_gov_au (en).
# Generated by scripts/migrate_i18n.py from the original PARAM_TRANSLATIONS
# and PARAM_DESCRIPTIONS dicts. Edit freely.
---
params:
  address:
    description: Street address with suburb (e.g. '100 Cedric Street, Stirling, WA,
      Australia')
    label: Street Address
  lat:
    description: Latitude (optional if address is provided)
  lon:
    description: Longitude (optional if address is provided)
