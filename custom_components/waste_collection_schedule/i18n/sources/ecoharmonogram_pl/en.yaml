# Per-source overrides for ecoharmonogram_pl (en).
# Generated by scripts/migrate_i18n.py from the original PARAM_TRANSLATIONS
# and PARAM_DESCRIPTIONS dicts. Edit freely.
---
params:
  additional_sides_matcher:
    description: Additional matcher for collection sides
    label: Additional Sides Matcher
  community:
    description: Community
    label: Community
  district:
    description: District
    label: District
  g1:
    description: Select Collection group to limit results to a specific group within
      a waste type (e.g. 'Papier (1 x miesiąc)' for Paper collected once a month).
      Leave empty and only fill if it fails, you'll then see a list of available groups.
    label: Group 1
  g2:
    description: Select Collection group to limit results to a specific group within
      a waste type (e.g. 'Papier (1 x miesiąc)' for Paper collected once a month).
      Leave empty and only fill if it fails, you'll then see a list of available groups.
    label: Group 2
  g3:
    description: Select Collection group to limit results to a specific group within
      a waste type (e.g. 'Papier (1 x miesiąc)' for Paper collected once a month).
      Leave empty and only fill if it fails, you'll then see a list of available groups.
    label: Group 3
  g4:
    description: Select Collection group to limit results to a specific group within
      a waste type (e.g. 'Papier (1 x miesiąc)' for Paper collected once a month).
      Leave empty and only fill if it fails, you'll then see a list of available groups.
    label: Group 4
  g5:
    description: Select Collection group to limit results to a specific group within
      a waste type (e.g. 'Papier (1 x miesiąc)' for Paper collected once a month).
      Leave empty and only fill if it fails, you'll then see a list of available groups.
    label: Group 5
  house_number:
    description: House number
    label: House number
  language:
    description: Language for waste type names (pl, en, uk, ru)
    label: Language
  street:
    description: Street
    label: Street
  town:
    description: Town
    label: Town
