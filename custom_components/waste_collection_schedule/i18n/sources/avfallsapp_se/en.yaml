# Per-source overrides for avfallsapp_se (en).
# Generated by scripts/migrate_i18n.py from the original PARAM_TRANSLATIONS
# and PARAM_DESCRIPTIONS dicts. Edit freely.
---
params:
  api_key:
    description: API key or device ID used by the mobile app.
    label: API key / device ID
  service_provider:
    description: Name of the Avfallsapp provider.
    label: Service provider
  street_address:
    description: Street address used for address lookup and registration.
    label: Street address
  token:
    description: Bearer token required by some providers.
    label: Bearer token
