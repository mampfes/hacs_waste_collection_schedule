# Per-source overrides for arun_gov_uk (en).
# Generated by scripts/migrate_i18n.py from the original PARAM_TRANSLATIONS
# and PARAM_DESCRIPTIONS dicts. Edit freely.
---
params:
  address:
    description: Address (legacy, use UPRN instead)
    label: Address (legacy, use UPRN instead)
  postcode:
    description: Postcode (legacy, use UPRN instead)
    label: Postcode (legacy, use UPRN instead)
  uprn:
    description: Unique Property Reference Number (UPRN)
    label: Unique Property Reference Number (UPRN)
