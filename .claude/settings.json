{
  "$comment": "Project-shared Claude Code settings. Personal permissions and personal hooks belong in .claude/settings.local.json (gitignored).",
  "hooks": {
    "PreToolUse": [
      {
        "$comment": "Blocks manual runs of update_docu_links.py and asks for confirmation before anything is posted to GitHub. See .claude/hooks/guard_bash.py.",
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python \"$CLAUDE_PROJECT_DIR/.claude/hooks/guard_bash.py\""
          }
        ]
      }
    ]
  }
}
